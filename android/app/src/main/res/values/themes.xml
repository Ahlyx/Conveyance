<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Post-splash application theme. Compose supplies the real UI
         styling via ConveyanceTheme; this only needs to be a bare,
         action-bar-free host. -->
    <style name="Theme.App" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Launch theme, per the androidx core-splashscreen contract:
         AndroidManifest points the application and MainActivity at this,
         and installSplashScreen() hands off to postSplashScreenTheme. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.App</item>
    </style>
</resources>
