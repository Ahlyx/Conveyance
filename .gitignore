/target
/fuzz/target
/.vscode
__pycache__/
*.db
*.db-wal
*.db-shm

# Local-only config used while developing; the real one lives in the
# platform config directory (see README "What it needs").
/config.toml

# Fuzzing artifacts
/fuzz/target

# Personal standing instructions (not project-specific)
ENGINEERING-GUARDRAILS.md

# ---- Android / Gradle (phase 10) ----
/android/.gradle/
/android/build/
/android/**/build/
/android/local.properties
/android/captures/
/android/.kotlin/
/android/app/release/
# Rust .so files cross-compiled by cargo-ndk (phase 10.1 UniFFI bridge);
# rebuilt from crates/conveyance-crypto-ffi on every Android build. Split
# per variant in phase 10.4 (debug carries `test-vectors`, release does not).
/android/app/src/main/jniLibs/
/android/app/src/debug/jniLibs/
/android/app/src/release/jniLibs/
.idea/
*.iml
# Signing material never belongs in git.
*.jks
*.keystore

# Phase exit reports: useful locally, kept out of the public history.
PHASE_*_EXIT*.md
